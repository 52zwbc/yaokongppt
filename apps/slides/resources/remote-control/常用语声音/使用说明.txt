把与「常用语.txt」每行文字同名的 mp3 录音放在此文件夹。

例如：「常用语.txt」里有一行「请物理课代表到办公室来。」，
则在这里放一个文件名为「请物理课代表到办公室来。.mp3」的录音。

信息呼叫时，软件会用手机端发送的文字去匹配同名 mp3：
- 找到：播放一次（录音长度由你自己的录音决定，软件不会重复播放）。
- 找不到：只显示置顶文字，不报错、不卡顿。

注意：录音文件名不要包含 \ / : * ? " < > | 等字符，
这些会被软件自动替换成下划线，导致文字与文件名匹配不上。
