# ykppt 对话记录

> 约定：此后每次对本项目（ykppt/遥控PPT）的修改，都把对话追加到本文件末尾。
> 基线版本：v0.6.0（git commit `v0.6.0 baseline`，hash `264b1e9`）。

---

## 2026-09-11（UTC）—— v0.6.1

### 用户需求原文

> 阅读 ykppt 文件夹里面的md文件了解这个项目，然后git记录版本，然后进行下个版本的修改：
> 文件 菜单新增 新建 项和 关闭 项。
> 开始选项卡的 停止远程控制 改名 远程控制，把它的弹出窗口中的 停止远程控制 按钮去掉。因为退出本软件会自然停止，这里的功能不需要
> 软件打开时只显示新建的空白文件，不再打开有一页提示的那个pptx
> 软件打开时显示一个窗口，提示 本软件完全免费使用，微信搜索小程序《52中文编程》可以查看使用帮助。如果喜欢请扫码支持一下，扫码后可以屏蔽此窗口，然后显示 微信赞赏2.jpg 这个图片，下面中间是关闭按钮，窗口右上角也可以关闭。屏蔽窗口的方式下个版本再实现，可以先预留相应的代码
> 修改完先构建appimage版本让我测试

### AI 做了什么

1. 阅读 `README-WINDOWS开发说明.md`、`UBUNTU构建提示.md`、`package.json` 了解项目（Electron + electron-vite + electron-builder，monorepo，主应用 `apps/slides` 包名 `yaokongppt`）。
2. 在 `ykppt/` 执行 `git init`，`.gitignore` 忽略 node_modules/release/out/dist，提交 `v0.6.0 baseline`。
3. 代码修改（版本升至 0.6.1）：
   - 文件菜单新增 新建/关闭：Ribbon 文件菜单（`Ribbon.tsx`，新建置顶带 Ctrl+N，关闭垫底）+ 原生应用菜单（`slides-main.ts buildSlidesMenu`，新建 Ctrl+N / 关闭）+ `MenuCommand` 新增 `'new' | 'close'`（`shared/ipc.ts`）+ `App.tsx` 接 `newFile`/`closeFile`（均走 `newBlank`，与"打开"一致不弹存盘提示）+ `Props` 新增 `onNew`/`onCloseFile`（`ribbon-shared.tsx`）+ ribbon 19 语言、main 19 语言文案补齐。
   - 远程控制改名：`RibbonHomeTab.tsx` 按钮恒显示"远程控制"（保留运行高亮）；`App.tsx` 弹窗删除"停止远程控制"按钮，只留"知道了"；主进程 stop 接口保留（退出时内部仍用）。
   - 启动只显示空白：删除 `slides-main.ts` 首次运行欢迎 pptx 逻辑（`FIRST_RUN_MARKER` / `isFirstRun` / `buildWelcomePptx` / `WELCOME_HELP_TEXT` 及启动注入块、`addElement` 孤立 import）。
   - 启动赞赏弹窗：`App.tsx` 新增 `donateOpen` 弹窗（免费说明文案 + 赞赏码图 + 底部居中"关闭" + 右上角 ×）；`微信赞赏2.jpg` 拷入 `src/renderer/assets/donate-qrcode.jpg`（vite 打包）；`styles.css` 新增 `.donate-modal-*` 样式；新建 `src/renderer/donate-suppress.ts` 预留屏蔽开关（`DONATE_MODAL_SUPPRESS_KEY` + `is/setDonateModalSuppressed` + TODO），本版本每次启动都弹。
4. 验证：`typecheck` 通过；`vitest` 45 文件通过、437 用例通过；确认打包产物含新字符串与赞赏图。
5. 构建：`npm install` → `rebuild-native` → `npm run dist:linux`，产物：
   - `apps/slides/release/遥控PPT-0.6.1.AppImage`（约 125MB，主测）
   - `apps/slides/release/yaokongppt_0.6.1_amd64.deb`（附带）
6. 0.6.1 改动暂未提交，待用户测试通过后再 commit。

### 用户反馈

> （待测试）
